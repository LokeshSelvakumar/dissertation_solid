/// <reference types="node" />
import { fetch } from "cross-fetch";
import { EventEmitter } from "events";
import { ITokenRefresher } from "../login/oidc/refresh/ITokenRefresher";
import { KeyPair } from "./dpopUtils";
export declare type RefreshOptions = {
    sessionId: string;
    refreshToken: string;
    tokenRefresher: ITokenRefresher;
};
export declare const DEFAULT_EXPIRATION_TIME_SECONDS = 600;
export declare type DpopHeaderPayload = {
    htu: string;
    htm: string;
    jti: string;
};
export declare function buildAuthenticatedFetch(unauthFetch: typeof fetch, accessToken: string, options?: {
    dpopKey?: KeyPair;
    refreshOptions?: RefreshOptions;
    expiresIn?: number;
    eventEmitter?: EventEmitter;
}): Promise<typeof fetch>;
